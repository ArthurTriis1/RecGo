<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ufpe.recife.tads.recgo">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecGO">
        <activity android:name=".UserProfileActivity"
            android:theme="@style/Theme.RecGO.NoActionBar"
            />
        <activity android:name=".ListAllLocalsActivity"
            android:theme="@style/Theme.RecGO.NoActionBar"/> <!-- <activity -->
        <!-- android:name=".ListAllLocalsActivity" -->
        <!-- android:label="@string/title_activity_list_all_locals" -->
        <!-- android:theme="@style/Theme.RecGO.NoActionBar" > -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".SearchLocalsActivity" -->
        <!-- android:label="@string/title_activity_search_locals" -->
        <!-- android:theme="@style/Theme.RecGO.NoActionBar"></activity> -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/Theme.RecGO.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>